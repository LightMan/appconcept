import React from 'react';
// import styles from './SectionWhat.module.scss';

const SectionSolutions: React.FC = (props) => {
  return (
    <div>Soluciones</div>
  );
}

export default SectionSolutions;
